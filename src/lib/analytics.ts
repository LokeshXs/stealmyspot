/**
 * Public read-only Umami dashboard, turned on with the "Share URL" toggle in
 * Umami's website settings. The footer and the masthead pill both point here,
 * so the address lives in one place rather than being typed twice.
 */
export const UMAMI_SHARE_URL = "https://cloud.umami.is/share/wVv7BnVISVeGHHNL";
